
# source my env

if [ -f ~/.zsh_my_env ]; then
    . ~/.zsh_my_env
fi

light-locker --lock-on-suspend &

xrdb -merge ~/.Xdefaults 

$HOME/bin/redshift_launcher start &

synclient TapButton1=0 TapButton2=0 TapButton3=0

exec awesome

