#!/usr/bin/env python3

import sys
import GeoIP

if __name__ == '__main__':
    gi = GeoIP.open("/usr/share/GeoIP/GeoIPCity.dat", GeoIP.GEOIP_STANDARD)
    coord = gi.record_by_addr(sys.argv[1])
    print("%.2f:%.2f" % (coord["latitude"], coord["longitude"]))
        
    
     